# K4-service dependencies
# Pin major versions for reproducibility.

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# K4 dependencies (must align with K4/requirements if it has one)
numpy>=1.24.0
scikit-learn>=1.4.0
torch>=2.0.0
sentence-transformers>=2.3.0
joblib>=1.3.0

# Optional: for async background tasks or richer monitoring
httpx>=0.26.0

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.26.0  # already listed
